name: Auto Cron Every 5 Minutes

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping-vercel:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel Cron Endpoint
        run: |
          curl https://macrometrics-v2.vercel.app/api/autosend/cron
